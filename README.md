# n8n Backup Manager

Автоматична система резервного копіювання та відновлення для n8n з підтримкою PostgreSQL та SQLite.

## 🚀 Можливості

- ✅ Автоматичне резервне копіювання n8n та бази даних
- ✅ Підтримка PostgreSQL та SQLite
- ✅ Гнучке планування бекапів (cron або інтервали)
- ✅ Завантаження та відновлення бекапів
- ✅ Захист важливих бекапів від автоматичного видалення
- ✅ Веб-інтерфейс для управління
- ✅ Система автоматичного оновлення
- ✅ Моніторинг статусу підключення

## 📋 Вимоги

- Docker та Docker Compose
- n8n запущений у Docker контейнері
- PostgreSQL або SQLite база даних

## 🛠️ Встановлення

### 1. Клонування репозиторію

\`\`\`bash
git clone https://github.com/aleksnero/n8n-backup-manager.git
cd n8n-backup-manager
\`\`\`

### 2. Запуск через Docker Compose

\`\`\`bash
docker-compose up -d --build
\`\`\`

### 3. Перше налаштування

1. Відкрийте браузер: `http://localhost:3000`
2. Натисніть "First Time Setup"
3. Створіть адміністратора (логін та пароль)
4. Увійдіть у систему

### 4. Налаштування підключення

Перейдіть у розділ **Settings** та вкажіть:

- **Database Container Name**: назва Docker контейнера з базою даних (наприклад, `postgres-1`)
- **Database Type**: PostgreSQL або SQLite
- **Database Path** (для SQLite): `/home/node/.n8n/database.sqlite`
- **Database credentials** (для PostgreSQL): user, password, database name
- **Backup Schedule**: виберіть інтервал або cron вираз
- **Protected Backups Count**: кількість бекапів для захисту від видалення

## 📖 Використання

### Dashboard
- Перегляд статусу підключення
- Інформація про останній бекап
- Час до наступного бекапу
- Швидкі дії (створення бекапу, завантаження)

### Backups
- Перегляд всіх бекапів
- Завантаження бекапів
- Відновлення з бекапу
- Захист/розблокування бекапів
- Видалення бекапів

### Settings
- Налаштування підключення до контейнера
- Конфігурація бази даних
- Планування автоматичних бекапів
- Налаштування ретенції

### Logs
- Перегляд логів системи
- Копіювання та завантаження логів
- Автоматичне оновлення

### Updates
- Перевірка наявності оновлень
- Перегляд release notes
- Застосування оновлень одним кліком
- Rollback до попередньої версії

## 🔄 Система оновлень

Backup Manager підтримує автоматичне оновлення:

1. Перейдіть у розділ **Updates**
2. Натисніть **Check for Updates**
3. Якщо доступна нова версія, натисніть **Apply Update**
4. Система автоматично завантажить, застосує оновлення та перезапуститься

У разі проблем можна відкотити оновлення через кнопку **Rollback**.

## 🐳 Docker Compose

\`\`\`yaml
version: '3.8'
services:
  backup-manager:
    build: .
    container_name: n8n-backup-manager
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./backups:/app/backups
      - ./data:/app/data
    environment:
      - JWT_SECRET=your_secret_here
\`\`\`

## 🔧 Змінні середовища

- `JWT_SECRET` - секретний ключ для JWT токенів
- `UPDATE_SERVER_URL` - URL для перевірки оновлень (за замовчуванням: GitHub)

## 📝 Ліцензія

MIT License - дивіться файл LICENSE для деталей

## 🤝 Внесок

Вітаються pull requests! Для великих змін спочатку відкрийте issue для обговорення.

## 📞 Підтримка

Якщо у вас виникли проблеми, створіть issue на GitHub: https://github.com/aleksnero/n8n-backup-manager/issues

## 🙏 Подяки

Створено для спільноти n8n з ❤️
